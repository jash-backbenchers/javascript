//Calculate Tip
function calculateTip() {
  //validate input
  //Calculate tip
  //round to two decimal places
  //next line allows us to always have two digits after decimal point
  //Display the tip
}

//Hide the tip amount on load

//click to call function (on click event listner)
